# Copy to .env and fill in. The offline demo (`npm run demo`) needs neither.
ANTHROPIC_API_KEY=
# Optional: an NCBI API key raises the PubMed rate limit from 3 to 10 req/s.
NCBI_API_KEY=
# Contact email sent with NCBI E-utilities requests (their etiquette policy).
NCBI_EMAIL=you@example.com
